# 在线教育平台本地部署文档

这份文档对应当前仓库的最新代码，适合直接在本地跑起来。

## 1. 运行环境

- Python 3.9+
- Node.js 18+
- npm 9+
- 可选：MySQL 8.0+

说明：
- 后端默认使用 SQLite，想快速跑通时不用先装 MySQL。
- 如果你要长期保存数据或模拟正式环境，可以切到 MySQL。

## 2. 项目结构

- `backend/`：Django 后端、模型、接口、上传、种子数据
- `frontend/`：Vue 3 + Vite 前台和后台页面
- `backend/sql/schema.sql`：MySQL 建表脚本

## 3. 快速部署

### 3.1 启动后端

```bash
cd backend
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py seed_demo
python manage.py runserver 0.0.0.0:8000
```

Windows 下激活虚拟环境：

```powershell
.venv\Scripts\activate
```

### 3.2 启动前端

```bash
cd frontend
npm install
npm run dev
```

访问地址：

- 前台：`http://127.0.0.1:5173/#/`
- 后台：`http://127.0.0.1:5173/#/admin`
- 后端：`http://127.0.0.1:8000/`

## 4. MySQL 部署

如果你想让后端使用 MySQL，按下面配置。

### 4.1 创建数据库

```sql
CREATE DATABASE IF NOT EXISTS online_education
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;
```

也可以直接导入建表脚本：

```bash
mysql -uroot -p online_education < backend/sql/schema.sql
```

### 4.2 配置 `backend/.env`

在 `backend/.env` 中写入：

```env
USE_MYSQL=1
MYSQL_DATABASE=online_education
MYSQL_USER=root
MYSQL_PASSWORD=你的密码
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
DJANGO_DEBUG=1
DJANGO_SECRET_KEY=change-me
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
```

然后执行：

```bash
cd backend
python manage.py migrate
python manage.py seed_demo
```

## 5. 默认账号

- 管理员：`admin / admin123`
- 教师：`teacher / teacher123`
- 学生：`student / student123`

## 6. 当前功能说明

- 前台和后台登录态已分离，不会互相踢下线
- 后台新增用户、公告、视频后会同步到前台数据源
- 视频支持本地上传到 `backend/media`
- 公告支持后台新增/删除/修改，前台公告页会实时读取后端数据

## 7. 启动顺序建议

1. 启动后端
2. 执行 `migrate`
3. 执行 `seed_demo`
4. 启动前端
5. 打开浏览器访问前台或后台

## 8. 常见问题

### 8.1 前台或后台空白

检查：
- 后端是否已经启动在 `8000`
- 前端是否已经启动在 `5173`
- 浏览器是否访问了 `#/` 或 `#/admin`

### 8.2 上传文件失败

检查：
- 后端是否启动
- `backend/media` 目录是否存在且可写
- 是否使用了管理员账号上传视频、公告等后台内容

### 8.3 后台看不到新用户/新公告

检查：
- 是否重新刷新页面
- 是否登录的是管理员账号
- 是否已经执行过后端接口保存，而不是只改了本地临时数据

### 8.4 MySQL 连接失败

检查：
- 数据库名是否为 `online_education`
- `MYSQL_USER` / `MYSQL_PASSWORD` 是否正确
- MySQL 服务是否已启动

## 9. 验证是否成功

登录后检查这些内容：

- 首页能正常展示
- 登录/注册可用
- 课程资料、在线视频、学习交流、教育资讯、网站公告都能加载
- 后台管理可以登录
- 新注册用户能出现在后台系统用户里
- 后台新增公告后，前台公告页能看到

