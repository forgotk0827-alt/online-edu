# 在线教育平台本地部署文档

本文档用于在 Windows 本地环境部署“在线教育平台”项目，包含后端 Django、前端 Vue 以及 MySQL 数据库配置。

## 1. 环境要求

### 1.1 基础环境
- 操作系统：Windows 10 / 11
- Python：3.10 及以上
- Node.js：18 及以上
- MySQL：8.0 及以上
- npm：随 Node.js 安装

### 1.2 已使用的技术栈
- 后端：Python + Django + PyMySQL
- 前端：Vue 3 + Vite + Element Plus
- 数据库：MySQL

## 2. 项目目录

- `backend/`：Django 后端接口、模型、迁移、种子数据
- `frontend/`：Vue 前台与后台管理页面
- `backend/sql/schema.sql`：MySQL 建表脚本
- `backend/.env`：后端数据库与运行环境配置

## 3. 数据库部署

### 3.1 启动 MySQL 服务
确认 MySQL 服务已启动。

PowerShell 可检查：

```powershell
Get-Service | Where-Object { $_.Name -like '*mysql*' }
```

### 3.2 创建数据库

使用 MySQL 客户端执行：

```sql
CREATE DATABASE IF NOT EXISTS online_education
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;
```

也可以直接运行项目中的建表脚本：

```powershell
mysql -uroot -p123456 < backend\sql\schema.sql
```

### 3.3 配置后端 `.env`

在 `backend/.env` 中填写：

```env
USE_MYSQL=1
MYSQL_DATABASE=online_education
MYSQL_USER=root
MYSQL_PASSWORD=123456
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
DJANGO_DEBUG=1
DJANGO_SECRET_KEY=change-me
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
```

说明：
- `USE_MYSQL=1`：强制后端使用 MySQL
- `MYSQL_DATABASE`：数据库名
- `MYSQL_USER` / `MYSQL_PASSWORD`：MySQL 登录账号和密码
- `MYSQL_HOST` / `MYSQL_PORT`：数据库地址与端口

## 4. 后端部署

### 4.1 创建虚拟环境

```powershell
cd backend
py -3 -m venv .venv
.venv\Scripts\activate
```

### 4.2 安装依赖

```powershell
pip install -r requirements.txt
```

如果 MySQL 驱动异常，可再执行：

```powershell
pip install PyMySQL
```

### 4.3 执行数据库迁移

```powershell
py -3 manage.py makemigrations
py -3 manage.py migrate
```

### 4.4 写入中文演示数据

```powershell
py -3 manage.py seed_demo
```

该命令会写入：
- 学生、教师、管理员账号
- 课程资料
- 在线视频
- 学习交流帖子
- 教育资讯
- 网站公告
- 轮播图
- 部分购买订单

### 4.5 启动后端服务

```powershell
py -3 manage.py runserver 0.0.0.0:8000
```

启动后访问：

```text
http://127.0.0.1:8000
```

## 5. 前端部署

### 5.1 安装依赖

```powershell
cd frontend
npm install
```

### 5.2 配置前端接口地址

默认前端接口地址指向：

```text
http://127.0.0.1:8000/api
```

如需修改，可在前端环境变量中设置：

```env
VITE_API_BASE=http://127.0.0.1:8000/api
```

### 5.3 启动前端开发服务

```powershell
npm run dev
```

启动后访问：

```text
http://127.0.0.1:5173
```

## 6. 默认测试账号

- 管理员
  - 用户名：`admin`
  - 密码：`admin123`
- 教师
  - 用户名：`teacher`
  - 密码：`teacher123`
- 学生
  - 用户名：`student`
  - 密码：`student123`

## 7. 启动顺序建议

推荐顺序如下：

1. 启动 MySQL 服务
2. 创建数据库 `online_education`
3. 配置 `backend/.env`
4. 安装后端依赖并执行 `migrate`
5. 执行 `seed_demo`
6. 启动 Django 后端
7. 安装前端依赖并启动 Vue 开发服务
8. 打开浏览器访问前端页面

## 8. 常见问题

### 8.1 提示数据库不存在
原因：MySQL 中没有创建 `online_education` 数据库。

解决：
```sql
CREATE DATABASE online_education DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 8.2 提示用户名或密码错误
原因：`backend/.env` 中的 MySQL 账号或密码不正确。

解决：
- 检查 `MYSQL_USER`
- 检查 `MYSQL_PASSWORD`
- 确认该账号有数据库访问权限

### 8.3 进入页面后接口报错
原因：
- 后端没有启动
- 前端接口地址配置错误
- 数据库迁移未执行

解决：
- 确认后端运行在 `8000`
- 确认前端 `VITE_API_BASE` 正确
- 确认执行过 `migrate` 和 `seed_demo`

### 8.4 上传文件失败
原因：
- 后端 `media` 目录权限不足
- 后端服务未启动

解决：
- 确认 `backend/media` 目录存在
- 确认 Django 正常运行

## 9. 验证部署是否成功

打开浏览器后检查以下内容：
- 首页可正常展示
- 登录/注册可用
- 课程资料、在线视频、学习交流、教育资讯、网站公告可正常加载
- 后台管理可正常登录
- MySQL 中已出现对应业务数据

---

如需把本文档导出为 Word 版，可直接复制到 Word 中排版。
